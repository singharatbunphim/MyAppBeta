import React, { useRef } from "react";
import { ImClipboard } from 'react-icons'

const addIcon = (props) => {

    const IconContainer = useRef(null);

    function addIcon(){
        const addIt = document.createElement(`${props.value.icons}`)
    }

    return (
        <>

        </>
    )
}


export default addIcon;